<head>
    <% include('partials/head') %>
</head>
<header>
    <% include('partials/header') %>
</header>
<h1>
  Sie haben eine Nachricht für das VISBO Center erhalten
</h1>
<p>
  <%if (userFrom.profile && userFrom.profile.firstName ) { %>
    <%= userFrom.profile.firstName %>
  <% } %>
  <%if (userFrom.profile && userFrom.profile.lastName ) { %>
    <%= userFrom.profile.lastName %>
  <% } %>
  (<%= userFrom.email %>) hat ihnen eine Nachricht für das VISBO Center: <b><%= vc.name %></b> gesendet.
</p>
<p>
  Nachricht: <br>
</p>
<h4>
  <%- message.replace(/\n/g, '<br/>')  %>
</h4>
<br>
<p>
  Um auf das Visbo Center zuzugreifen, folgen Sie dem Link: <b><a href="<%= url %>">View VISBO Center</a></b>
</p>
<p>
  Benutzen Sie Ihre e-Mail Adresse <%= userTo.email %> für die Anmeldung.
</p>
<footer>
    <% include('partials/footer') %>
</footer>
