<head>
    <% include partials/head %>
</head>
<header>
    <% include partials/header %>
</header>
<h1>
  Sie wurden in ein Visbo Projekt eingeladen
</h1>
<p>
  <%if (userFrom.profile && userFrom.profile.firstName ) { %>
    <%= userFrom.profile.firstName %>
  <% } %>
  <%if (userFrom.profile && userFrom.profile.lastName ) { %>
    <%= userFrom.profile.lastName %>
  <% } %>
  (<%= userFrom.email %>) hat Sie in das Visbo Projekt: <b><%= vp.name %></b> mit <%= vp.vpvCount %> Versionen eingeladen.
</p>
<p>
  Nachricht: <br>
  <h5>
    <%- message.replace(/\n/g, '<br/>')  %>
  </h5>
</p>
<h4>
  Bitte registrieren Sie sich bei Visbo
</h4>
<p>
  Um auf das Visbo Projekt zuzugreifen, registrieren Sie sich bei Visbo zuerst. Benutzen Sie bitte den folgenden Link: <a href="<%= url %>">Register</a>
</p>
<p>
  Benutzen Sie Ihre e-Mail Adresse <%= userTo.email %> für die Registrierung.
</p>
<footer>
    <% include partials/footer %>
</footer>
